javascript:(async()=>{const modifyTitle = (title) => {return title.replace(/ - Google (Docs|Sheets|Slides)$/, '');};const url = window.location.href;const title = modifyTitle(document.title);const markdown = `[${title}](${url})`;const html = `<a href="${url}">${title}</a>`;try {if (navigator.clipboard && navigator.clipboard.write) {await navigator.clipboard.write([new ClipboardItem({'text/plain': new Blob([markdown], { type: 'text/plain' }),'text/html': new Blob([html], { type: 'text/html' })})]);} else {throw new Error('Clipboard API not available');}} catch (err) {const textArea = document.createElement('textarea');textArea.value = markdown;textArea.style.position = 'fixed';textArea.style.opacity = '0';document.body.appendChild(textArea);textArea.select();const successful_copy = document.execCommand('copy');document.body.removeChild(textArea);if (!successful_copy) {alert('Fallback: Copy command was unsuccessful');}}})();